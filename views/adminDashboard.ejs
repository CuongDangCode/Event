<!DOCTYPE html>
<html>
<head>
  <title>Admin Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background: #f4f8fb; min-height: 100vh; display: flex; align-items: center; }
    .dashboard-container {
      background: #fff;
      padding: 36px 32px 28px 32px;
      border-radius: 12px;
      box-shadow: 0 4px 24px rgba(0,0,0,0.07);
      width: 100%;
      max-width: 420px;
      margin: auto;
      text-align: center;
    }
    .dashboard-title {
      color: #007bff;
      font-weight: 700;
      margin-bottom: 28px;
    }
    .dashboard-link {
      display: block;
      margin: 18px 0;
      color: #007bff;
      text-decoration: none;
      font-weight: 600;
      font-size: 1.1rem;
      transition: color 0.2s;
    }
    .dashboard-link:hover {
      color: #0056b3;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="dashboard-container">
    <h2 class="dashboard-title">Bảng điều khiển quản trị</h2>
    <a href="/admin/registrations" class="dashboard-link">Xem danh sách đăng ký</a>
    <a href="/logout" class="dashboard-link">Đăng xuất</a>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
